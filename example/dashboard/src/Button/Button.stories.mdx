<!-- Button.stories.mdx-->

import { Meta, Story } from '@storybook/addon-docs';

import { Button } from './Button';

<Meta title="Common/Button" component={Button}  />

export const Template = (args) => <Button {...args} />

# Button

<Canvas>
  <Story name="Primary">
    <Button>Add page</Button>
  </Story>
  <Story name="Secondary" args={{ 
    color: 'secondary',
    children: 'Ok',
  }}>>
    {Template.bind({})}
  </Story>
  <Story name="Transparent" args={{ 
    color: 'transparent',
    children: 'CANCEL',
  }}>>
    {Template.bind({})}
  </Story>
</Canvas>
