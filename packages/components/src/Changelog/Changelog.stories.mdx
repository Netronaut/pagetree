import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { Changelog } from './Changelog.tsx';

import mockPage from './__mocks__/page.json';

<Meta title="Changelog" component={Changelog} />

# Changelog

<Canvas>
  <Story
    name="Changelog open"
    args={{
      isChangelogOpen: true,
      page: mockPage,
    }}
  >
  {({ page, isChangelogOpen }) => (
    <Changelog page={page} isChangelogOpen={isChangelogOpen} />
  )}
  </Story>
</Canvas>
